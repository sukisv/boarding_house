import 'package:flutter/material.dart';

class ManagePropertiesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Manage Properties')),
      body: Center(child: Text('This is the Manage Properties page.')),
    );
  }
}
